name: CI-CD Website_KhanhHoaTravel

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build & Push Backend
      - name: Build and push Backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/flask-backend:latest

      # Build & Push Frontend
      - name: Build and push Frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/react-frontend:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      # BƯỚC MỚI 1: Lấy mã nguồn từ GitHub về máy ảo Action để có file docker-compose.yml
      - name: Checkout code
        uses: actions/checkout@v3

      # BƯỚC MỚI 2: Đẩy file docker-compose.yml và thư mục nginx lên server
      - name: Copy config files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          # Gửi file docker-compose và toàn bộ thư mục nginx (chứa default.conf)
          source: "docker-compose.yml,nginx/"
          target: "/root/webapp"

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            mkdir -p /root/webapp
            cd /root/webapp
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            cat <<EOF > .env
            ${{ secrets.ENVIRONMENT }}
            EOF

            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f

            echo "Đang đợi database (postgesSQL-container) khởi chạy........"
            sleep 5
            docker compose exec -T backend flask db upgrade
